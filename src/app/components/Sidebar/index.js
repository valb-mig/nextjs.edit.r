import SidebarRoot from "./config/Root";
import SidebarBody from "./config/Body";
import SidebarSection from "./config/Section";

const Sidebar = {
  Root: SidebarRoot,
  Body: SidebarBody,
  Section: SidebarSection,
};

export default Sidebar;
