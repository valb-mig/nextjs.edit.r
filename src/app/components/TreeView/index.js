import RootTreeView from "./config/Root";
import FolderTreeView from "./config/Folder";
import FileTreeView from "./config/File";

const TreeView = {
    Root: RootTreeView,
    Folder: FolderTreeView,
    File: FileTreeView,
};

export default TreeView;
